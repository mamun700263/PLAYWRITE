[project]
name = "playwrite"
version = "0.1.0"
description = "Elite-grade scraping and orchestortion system"
requires-python = ">=3.10"
dependencies = ["pydantic", "pytest", "playwright"]

[project.scripts]
playwirght = "app.runner:main"


[tool.setuptools.packages.find]
where = ["."]